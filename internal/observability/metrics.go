package observability

// Temporarily disabled prometheus metrics due to dependency issues
// var (
//   PollErrors = prometheus.NewCounterVec(...)
// )

// Init initializes observability (currently no-op)
func Init() {
	// TODO: Re-enable prometheus metrics when dependencies are resolved
}
